---

classes:
  - basenode
  - telegraf

basenode::packages::absent:
  - postfix

wf_report_tags:
  - status
  - git_rev
  - run_by
  - puppet_version
  - environment
  - role
  - product

wavefront_server: metrics.wavefront.com
