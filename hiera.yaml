---
:backends:
  - yaml

:hierarchy:
  - "%{::environment}/%{::hostname}"
  - "%{::environment}/role.%{::role}"
  - "%{::environment}/mod.%{module_name}"
  - "%{::environment}/common"
  - "%{::hostname}"
  - "role.%{::role}"
  - "mod.%{module_name}"
  - common


:yaml:
  :datadir: hieradata
