---
:backends:
  - yaml

:hierarchy:
  - common
  - "%{::environment}/%{::hostname}"
  - "%{::environment}/role.%{::role}"
  - "%{::environment}/mod.%{module_name}"
  - "%{::environment}/common"

:yaml:
  :datadir: hieradata
