---

version: 5

defaults:
  datadir: hieradata
  data_hash: yaml_data

hierarchy:
  - name: "standard hierarchy"
    paths:
      - "%{facts.environment}/role.%{facts.role}.yaml"
      - "%{facts.environment}/common.yaml"
      - "role.%{facts.role}.yaml"
      - "common.yaml"
